import React from 'react';
import PagePlaceholder from '../../components/dev/PagePlaceholder';

export default function CourseCatalog() {
  return <PagePlaceholder routePath="/courses" owner="M2 — Marketing & Discovery" screenshots={['Course catalog.png', 'Course catalog-mobile.png']} />;
}
