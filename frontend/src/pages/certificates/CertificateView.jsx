import React from 'react';
import PagePlaceholder from '../../components/dev/PagePlaceholder';

export default function CertificateView() {
  return <PagePlaceholder routePath="/certificate/:certId" owner="M3 — Cart, Checkout, Orders & Certificates" screenshots={['certificate.png']} />;
}
